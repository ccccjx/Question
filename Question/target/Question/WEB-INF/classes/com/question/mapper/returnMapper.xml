<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.question.dao.ReturnDao">
<update id="updateSelectionCount" parameterType="int">
    UPDATE tb_selection SET COUNT=COUNT+1 WHERE selection_id=#{0}
</update>
</mapper>